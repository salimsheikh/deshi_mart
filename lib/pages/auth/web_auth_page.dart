import 'package:flutter/material.dart';

class WebAuthPage extends StatelessWidget {
  const WebAuthPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Web Auth Page"),
      ),
    );
  }
}
